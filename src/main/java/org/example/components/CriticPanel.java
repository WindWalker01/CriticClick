package org.example.components;

import org.example.Settings;

import javax.swing.*;

public class CriticPanel extends JPanel {

    public CriticPanel() {
        setBounds(0, 0, Settings.WINDOW_WIDTH, Settings.WINDOW_HEIGHT);
        setLayout(null);
    }
}
